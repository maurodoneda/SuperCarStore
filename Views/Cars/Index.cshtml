@model IEnumerable<SuperCarStore.Models.Car>

@{
    ViewBag.Title = "Cars";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <div class="text-center pages container">




        @using (Html.BeginForm())
        {
            <p>
                @Html.TextBox("searchString","", new {placeholder="Search by Make"})
                 @Html.DropDownList("Store", "Search By Store")
                <input type="submit" value="Search" />
            </p>

        }


        <p>
            @Html.ActionLink("Add new car to stock", "Create")
        </p>
        <p>
            <a href="/Home/Index">Back to Home Page</a>
        </p>

        <h2>Here is our Supercars available for you!</h2>
        <br />

        @foreach (var item in Model)
        {
            <div class="container cardContainer mb-0">
                <div class="card">
                    <div class="row no-gutters cardBody" id="carBody">
                        <div class="col-auto" id="carImg">
                            <img src="~/Content/imgs/cars/@Html.DisplayFor(modelItem => item.ImgUrl)" class="cardImg" />
                        </div>
                        <div class="col">
                            <div class="card-block">
                                <dl class="dl-horizontal cardDetails">

                                    <dt>
                                        Year
                                    </dt>

                                    <dd>
                                        @Html.DisplayFor(modelItem => item.Year)
                                    </dd>

                                    <dt>
                                        Engine
                                    </dt>

                                    <dd>
                                        @Html.DisplayFor(modelItem => item.EngineSpec)
                                    </dd>

                                    <dt>
                                        Horse Power(HP)
                                    </dt>

                                    <dd>
                                        @Html.DisplayFor(modelItem => item.HP)
                                    </dd>
                                    <dt>
                                        Max Speed(mph)
                                    </dt>

                                    <dd>
                                        @Html.DisplayFor(modelItem => item.TopSpeed)
                                    </dd>



                                </dl>

                                <div class="descContainer">

                                    <p class="carMake">@Html.DisplayFor(modelItem => item.Make)</p>
                                    <p class="carModel">@Html.DisplayFor(modelItem => item.Model)</p>
                                    <div class="container description overflow-auto" id="carDescription">
                                        Contrary to popular belief, Lorem Ipsum is not simply random text. It has roots in a piece of classical Latin literature from 45 BC, making it over 2000 years old. Richard McClintock, a Latin professor at Hampden-Sydney College in Virginia.
                      
                                    </div>
                                </div>
                                <div class="actionBtns">
                                    @*<a class="text-info mr-1 likeBtn">
                                        <i class="fas fa-thumbs-up fa-2x"></i>
                                    </a>*@
                                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class = "btn btn-primary mt-2" })
                                    @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-info mt-2" })
                                    @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class = "btn btn-danger mt-2" })
                                    @Html.ActionLink("Book", "Details", new { id = item.Id }, new { @class = "btn btn-primary mt-2" })

                                    @*<a class="text-danger mr-1">
                                        <i class="fas fa-thumbs-down fa-2x"></i>
                                    </a>*@
                                </div>


                            </div>

                        </div>
                    </div>
                </div>
            </div>
            <div class="d-flex bottomInfo justify-content-center pt-3 mb-3 mt-0">

                <p class="mr-2">
                    Experience Price($/Hour):
                </p>

                <p class="mr-2 carInfo">
                    @Html.DisplayFor(modelItem => item.RentalPrice)
                </p>
                |
                <br />
                <p class="ml-2 mr-2">
                    Store Location:
                </p>

                <p class="carInfo">
                    @Html.DisplayFor(modelItem => item.Store.City)
                </p>

            </div>
        }

    </div>

@Scripts.Render("~/Scripts/myScript.js")


